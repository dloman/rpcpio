load("@rules_proto//proto:defs.bzl", "proto_library")
load("@protobuf//bazel:cc_proto_library.bzl", "cc_proto_library")

package(default_visibility = ["//visibility:private"])

# Official gRPC interop proto definitions (vendored subset).
proto_library(
    name = "grpc_testing_proto",
    srcs = ["grpc_testing.proto"],
)

cc_proto_library(
    name = "grpc_testing_cc_proto",
    deps = [":grpc_testing_proto"],
)

genrule(
    name = "grpc_testing_rpcpio_gen",
    srcs = ["grpc_testing.proto"],
    outs = [
        "grpc_testing.rpcpio.pb.h",
        "grpc_testing.rpcpio.pb.cc",
    ],
    cmd = """
        $(location @protobuf//:protoc) \
            --plugin=protoc-gen-rpcpio=$(location //:rpcpio_cpp_plugin) \
            --rpcpio_out=$(RULEDIR) \
            -I$$(dirname $(location grpc_testing.proto)) \
            $(location grpc_testing.proto)
    """,
    tools = [
        "@protobuf//:protoc",
        "//:rpcpio_cpp_plugin",
    ],
)

cc_library(
    name = "grpc_testing_rpcpio",
    srcs = [":grpc_testing_rpcpio_gen"],
    hdrs = ["grpc_testing.rpcpio.pb.h"],
    copts = ["-std=c++20"],
    deps = [
        ":grpc_testing_cc_proto",
        "//:rpcpio_runtime",
    ],
)

cc_test(
    name = "interop_client_test",
    srcs = ["interop_client_test.cc"],
    copts = ["-std=c++20"],
    deps = [
        ":grpc_testing_rpcpio",
        "@googletest//:gtest_main",
    ],
    # Set TEST_SERVER_HOST and TEST_SERVER_PORT env vars to point at a server.
    tags = ["requires-network"],
)

cc_test(
    name = "interop_server_test",
    srcs = ["interop_server_test.cc"],
    copts = ["-std=c++20"],
    deps = [
        ":grpc_testing_rpcpio",
        "@googletest//:gtest_main",
    ],
    tags = ["requires-network"],
)
