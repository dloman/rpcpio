package(default_visibility = ["//visibility:public"])

cc_library(
    name = "asio_grpc_hdrs",
    hdrs = glob(["asio_grpc/**/*.h"]),
    strip_include_prefix = "/include",
    deps = [
        "@nghttp2-asio//:nghttp2_asio",
        "@protobuf//:protobuf",
    ],
    copts = ["-std=c++20"],
)
